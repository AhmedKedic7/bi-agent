{
  "model": {
    "name": "gemini-2.0-flash"
  },
  "mcpServers": {
    "postgres": {
      "command": "npx",
      "args": [
        "-y",
        "@modelcontextprotocol/server-postgres@0.6.2",
        "${POSTGRES_CONNECTION_STRING}"
      ]
    }
  }
}